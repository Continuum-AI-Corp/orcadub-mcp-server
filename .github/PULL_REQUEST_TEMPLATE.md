## What

<!-- What changed and why — the problem and the approach, not a file list. -->

## Verification

<!-- Commands you ran and their results, e.g.:
- [ ] `make check` (fmt + lint + test + vet + build + npm syntax checks)
- [ ] stdio smoke: `--version` and initialize handshake
-->

## Behaviour impact

<!-- Anything touching the AGENTS.md invariants (auth, gateway routing,
tool surface, delivery, billing-relevant paths) — call it out explicitly.
Write "none" otherwise. -->

Closes #
